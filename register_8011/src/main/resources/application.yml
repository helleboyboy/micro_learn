server:
  port: 8011

spring:
  application:
    name: register-server # 定义微服务名

  datasource:
    password: thinker@
    username: root
    url: jdbc:mysql://localhost:3306/micro_service
    driver-class-name: com.mysql.jdbc.Driver
  cloud:
    nacos:
      server-addr: only:8848
      discovery:
        cluster-name: gz


#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8016/eureka/