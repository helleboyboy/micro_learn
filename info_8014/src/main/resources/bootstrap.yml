spring:
  application:
    name: info-server  # 服务名
  profiles:
    active: test # 环境名
  cloud:
    nacos:
      server-addr: http://only:8848  # nacos地址
      config:
        file-extension: yaml # 文件后缀名
        namespace: 5ed0616b-e281-4855-89ec-6734a2421f50 # test namespace id 默认为public，如果不是public，需要标注，否则找不到对应的配置文件